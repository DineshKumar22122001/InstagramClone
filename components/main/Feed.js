import React from "react";
import { View, Text } from "react-native";

const Feed = () => {
  return (
    <View name="feed" options={{ headerShown: true }}>
      <Text>Feed</Text>
    </View>
  );
};

export default Feed;
